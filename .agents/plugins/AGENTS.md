# Contents

- `marketplace.json` registers repository-distributed Codex plugins in display order.

# Guidelines

- Keep the marketplace name stable and preserve existing plugin order unless a deliberate catalog change requires otherwise.
- Keep every plugin source repository-relative and aligned with its folder and manifest name.
- Include explicit installation, authentication, and category policy for every entry.
- Validate the marketplace and each referenced plugin before handoff.
