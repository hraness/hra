import * as stylex from "@stylexjs/stylex";

export const iconStyles = stylex.create({
  glyph: {
    display: "block",
    height: "1.25rem",
    verticalAlign: "middle",
    width: "1.25rem",
  },
});
