# Cross-platform daily driver

Status: initial macOS controller fixtures passed; production integration remains pending. Oompa is not yet qualified for full-time daily use.

Tracking issue: https://github.com/hraness/oompa/issues/187.

The owner selected macOS for their own execution and native macOS, Linux and Windows support for the product on 2026-09-11. This extends the existing product plan. It does not mark unfinished routing, usage, hosted, or rename work complete.

## Current evidence

The v0.8.0 GitHub artifact is published from `2dae06babe263b22bd6f382b3f92018ebb80a25a`. Its immutable artifact admission is separate from daemon rollout. Current source requires protected two-pass capacity evidence, its exact activation receipt, and intended-target marker proofs before current hosted writers are available.

macOS and Linux have installer and local storage implementations. Managed Claude authentication and new managed sessions remain Linux-only. Existing exact personal Claude bindings have a separate macOS continuation path; that exception does not establish managed profile isolation. Native Windows installation, private-file custody, local transport, process identity, protected terminal I/O and release qualification remain incomplete. CI currently covers macOS and Ubuntu.

The 2026-09-11 supported live capacity audit still reports one identity missing authority-reduction capacity and one service debt, with no lifecycle, terminal, orphan-cleanup or topology debt. It performs no reservation and therefore does not clear the historical hard-quota blocker. No repair or activation occurred. Hosted release attestation still names a predecessor, and four required canonical configuration names are absent.

The initial source-only macOS controller passes 17 native fixture cases, including pre-exec gating, malformed control, cancellation, deadlines, owner control EOF, helper death and the survival of a deliberately detached finite descendant. All fixture processes were collected. The 17 pure protocol tests and repository TypeScript, scoped lint and existing CI-equivalence contracts pass. This evidence does not admit provider I/O, managed Claude authentication, Windows or a production writer.

## Invariants

- Preserve one exact provider writer for a session, durable intent before dispatch, and generation-bound recovery without speculative replay.
- Keep credentials inside provider-owned profiles or protected local custody. Provider payloads, credential values and user transcripts are not validation artifacts.
- Separate ownership of the provider controller from containment of provider-created tools. A direct-child exit proof is not a namespace-wide cleanup proof.
- Keep hosted capacity, identity, migration and activation guards. No data erasure, quota increase, platform-check deletion or ad hoc process kill substitutes for a reviewed recovery path.
- Preserve stored identities and exact installer recovery evidence across the rename and platform work.

## Delivery order and acceptance

1. **Establish the actual operational state. In progress.** Record canonical app and hosted source/configuration evidence, inspect the bound capacity state through the supported operator, and resolve supported recovery before activation. A public page loading does not complete this step.
2. **Prove native controller ownership. In progress.** Add a closed protocol and a macOS native helper exercised only with credential-free fixtures. Prove admission before execution, exact child identity, deadline and pipe closure, interrupt/termination, owner death and recovery refusal. Preserve the existing Linux authority-containment implementation and its stronger contract. Specify a Windows process-handle and Job Object backend against the same direct-child contract, with separate containment capability evidence.
3. **Qualify managed Claude on macOS. Blocked on step 2 and live prerequisites.** Admit the exact pinned executable, independently authenticate two isolated profiles, prove detached credential/status isolation and interrupted-login recovery, and run the required session and interaction matrix. Change the closed platform policy only after that evidence is admitted. Personal-session adoption remains separately tested.
4. **Implement and qualify native Windows. Planned.** Provide the native launcher, owner SID/DACL and reparse-point protections, exact file identity and atomic publication, authenticated IPC, process handles and recovery, and protected console/file operations. Add native Windows CI and archive install/update/recovery qualification while preserving existing macOS/Linux coverage. Emulated platform tests alone cannot complete this step.
5. **Install and qualify the owner's Mac. Blocked on applicable earlier steps.** Install the admitted exact release through the supported installer, preserve the prior command and local history, and establish daemon/target activation proofs. Exercise both providers with real authentication: create and adopt sessions, stream, approve/deny, interrupt, attach files, restart, recover uncertainty, and verify local/remote continuity. Use a dedicated disposable acceptance session for effects.
6. **Close the product contract. Pending.** Reconcile public availability/support claims with actual evidence, finish the routing and usage capabilities already required by their active plans, and run exact-current-source release and production gates. Record remaining exceptions explicitly. Organization-wide guidance renames remain separately tracked and do not establish product readiness.

## Validation ownership

Each implementation worker owns focused tests and native fixture cleanup. One integration owner reviews the complete joined diff, the independent review, and the repository's unchanged aggregate CI gate on the exact candidate. Each OS needs its own native evidence. Authenticated acceptance, release admission, hosted activation and the owner-machine proof remain separate results.
